import React from "react";

type Params = { id: string };

const JourneyDetail = ({ params }: { params: Params }) => {
  // get journey by id
  return <main>JourneyDetail</main>;
};

export default JourneyDetail;
